diff --git a/chrome/browser/prefs/browser_prefs.cc b/chrome/browser/prefs/browser_prefs.cc
index 91971570758925d411850959585137f0ef368428..4958d51bd525b359afb0efd22ad768bfcbec1cb4 100644
--- a/chrome/browser/prefs/browser_prefs.cc
+++ b/chrome/browser/prefs/browser_prefs.cc
@@ -1433,6 +1433,7 @@ void RegisterProfilePrefs(user_prefs::PrefRegistrySyncable* registry,
   accessibility_prefs::RegisterInvertBubbleUserPrefs(registry);
   RegisterBrowserViewProfilePrefs(registry);
 #endif
+brave::RegisterLocalStatePrefs(registry);
 
 #if BUILDFLAG(ENABLE_SIDE_SEARCH)
   side_search_prefs::RegisterProfilePrefs(registry);
@@ -1447,6 +1448,7 @@ void RegisterProfilePrefs(user_prefs::PrefRegistrySyncable* registry,
   registry->RegisterBooleanPref(prefs::kPrivacyReviewShowWelcomeCard, true);
 
   RegisterProfilePrefsForMigration(registry);
+  brave::RegisterProfilePrefs(registry);
 }
 
 void RegisterUserProfilePrefs(user_prefs::PrefRegistrySyncable* registry) {
